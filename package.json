{
  "name": "chatsounds-converter",
  "private": true,
  "scripts": {
    "start": "yarn --production=true workspace chatsounds-converter-server run start",
    "start:dev": "yarn workspace chatsounds-converter-server run start:dev",
    "dev": "yarn run typecheck && yarn run build:dev && yarn run start:dev",
    "build": "yarn --production=true workspaces run build",
    "build:dev": "yarn workspaces run build:dev",
    "typecheck": "yarn workspaces run typecheck"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
